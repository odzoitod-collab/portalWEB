// Заглушка для Gemini API сервиса
export const generateNFTDescription = async (title: string): Promise<string> => {
  // Простая заглушка, возвращающая описание на основе названия
  return `Уникальный цифровой подарок "${title}". Эксклюзивный NFT для вашей коллекции.`;
};
